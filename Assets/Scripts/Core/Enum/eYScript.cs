using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class eYScript
{
    public enum type
    {
        none, scenario, subScenario, flag
    }
    public enum exe
    {
        none, first, always
    }
    public enum flag
    {
        equal, greater, less
    }
    public enum exeType
    {
        none, 決定キーで実行 ,接触実行,
    }
}
